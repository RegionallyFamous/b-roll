{
    "name": "b-roll",
    "version": "0.0.0-dev",
    "private": true,
    "description": "Dev tooling for the B-Roll WordPress plugin. Not shipped in the plugin zip.",
    "license": "GPL-2.0-or-later",
    "scripts": {
        "lint": "bin/lint-js",
        "smoke": "bin/smoke-playground",
        "build": "bin/build-zip"
    },
    "devDependencies": {
        "@wp-playground/cli": "^1.0.0",
        "eslint": "^8.57.0"
    }
}
